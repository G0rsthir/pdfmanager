apiVersion: v1
kind: Service
metadata:
  name: {{ include "pdfmanager.fullname" . }}
  labels:
    {{- include "pdfmanager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pdfmanager.selectorLabels" . | nindent 4 }}
